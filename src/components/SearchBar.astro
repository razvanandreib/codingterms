---
import { getCollection } from "astro:content";

import Search from "./Search";
const allPosts = await getCollection("terms");
const posts = allPosts.filter((post) => !post.data.draft && post.data.slug);

import RandomTerm from "./RandomTerm.astro";
---

<Search client:load searchList={posts} />
